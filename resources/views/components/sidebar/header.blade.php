<div class="flex items-center justify-between flex-shrink-0 shadow-md border-b-[1px] border-black">
  <!-- Logo -->
  <div class="pb-4">
    <h2 class="text-white font-semibold text-2xl text-center leading-8" x-transition x-show="isSidebarOpen || isSidebarHovered">
      KLINIK UTAMA ALIYAH MEDIKA
    </h2>
  </div>

  <!-- Toggle button -->
  {{-- <x-button type="button" icon-only sr-text="Toggle sidebar" variant="secondary" x-show="isSidebarOpen || isSidebarHovered"
    x-on:click="isSidebarOpen = !isSidebarOpen">
    <x-icons.menu-fold-right x-show="!isSidebarOpen" aria-hidden="true" class="hidden w-6 h-6 lg:block" />

    <x-icons.menu-fold-left x-show="isSidebarOpen" aria-hidden="true" class="hidden w-6 h-6 lg:block" />

    <x-heroicon-o-x aria-hidden="true" class="w-6 h-6 lg:hidden" />
  </x-button> --}}
</div>
